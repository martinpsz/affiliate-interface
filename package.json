{
  "dependencies": {
    "@lit-labs/context": "^0.2.0",
    "lit": "^2.6.1"
  },
  "devDependencies": {
    "es-dev-server": "^2.1.0"
  },
  "scripts": {
    "compile": "tsc --watch",
    "start": "npx es-dev-server --node-resolve --watch"
  }
}
